{
  "name": "capacitor-native-log",
  "version": "0.2.0",
  "description": "Alternative to Capacitor‘s Console plugin for logging to the native device log console",
  "main": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf ./dist",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "Michael Lawlor",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^1.5.2"
  },
  "devDependencies": {
    "typescript": "^3.2.4",
    "@capacitor/ios": "^1.5.2",
    "@capacitor/android": "^1.5.2"
  },
  "files": [
    "dist/",
    "ios/",
    "android/",
    "CapacitorNativeLog.podspec"
  ],
  "keywords": [
    "capacitor",
    "plugin",
    "native"
  ],
  "capacitor": {
    "ios": {
      "src": "ios"
    },
    "android": {
      "src": "android"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Mote-OU/capacitor-native-log"
  },
  "bugs": {
    "url": "https://github.com/Mote-OU/capacitor-native-log/issues"
  }
}
